# Cloudflare Pages configuration for MigriGuide frontend
# BACKEND_URL is set as an environment variable in the Cloudflare Pages dashboard
# (Settings → Environment variables → Production)

name = "migriguide"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

pages_build_output_dir = ".vercel/output/static"

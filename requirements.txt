# Runtime dependencies — what the API container installs.
# The pipeline additionally needs requirements-pipeline.txt.

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.1

openai>=1.50.0
pinecone>=9.0.0
